node_modules
dist
*.local
.idea
.DS_Store

# tsc -b (project build mode) incremental cache — machine-specific, not reproducible
*.tsbuildinfo

# third-party skills installed locally (MengTo) — keep on disk, do not track
.claude/
.superpowers/
docs/

